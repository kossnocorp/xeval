/**
 * @prompt
 * @eval math
 * @dataset ./data.csv
 */
const mathPrompt = `You are a math expert. Solve the following problem step-by-step and provide the final answer, here's the equation:
{{a}} + {{b}} = ?`;
